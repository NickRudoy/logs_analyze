# AI-анализатор безопасности веб-трафика

AI-анализатор использует GigaChat API для глубокого анализа результатов из Excel-отчётов, сгенерированных `analyze_direct_traffic.py`. Инструмент проводит экспертный анализ угроз безопасности, оценивает влияние на бизнес и генерирует конкретные рекомендации по защите.

## Возможности

- 🔍 **Анализ угроз безопасности** — идентификация типов атак (DDoS, сканирование, парсинг, боты), оценка уровня угрозы, выявление координированных атак
- 💼 **Оценка влияния на бизнес** — анализ влияния на пользовательский опыт, SEO, нагрузку на инфраструктуру, репутационные риски
- 🛡️ **Генерация рекомендаций** — конкретные действия (немедленные, краткосрочные, долгосрочные) с примерами команд и конфигураций
- 📊 **Executive Summary** — краткая сводка для руководства на понятном языке с фокусом на бизнес-ценность

## Требования

- **Python 3.13** (обязательно, не 3.14+)
  - Библиотека `gigachat` использует Pydantic V1, который несовместим с Python 3.14+
  - Если у вас установлен Python 3.14, создайте отдельное виртуальное окружение с Python 3.13
- Ключ авторизации (Authorization Key) из личного кабинета GigaChat API
- Excel-отчёт, сгенерированный `analyze_direct_traffic.py`

## Установка

### Важно: Требуется Python 3.13

Библиотека `gigachat` использует Pydantic V1, который несовместим с Python 3.14+. Для работы AI-анализатора необходим **Python 3.13** или ниже.

**Примечание:** Если виртуальное окружение `venv` уже создано в директории `ai_env`, вы можете сразу активировать его и установить зависимости (см. шаги 4-7 ниже). Если окружения нет или нужно пересоздать его с правильной версией Python, следуйте инструкциям ниже.

### Создание виртуального окружения с Python 3.13

#### Вариант 1: Если Python 3.13 уже установлен

1. Проверьте наличие Python 3.13:
```bash
python3.13 --version
```

2. Перейдите в директорию `ai_env`:
```bash
cd ai_env
```

3. Создайте виртуальное окружение с Python 3.13:
```bash
python3.13 -m venv venv
```

4. Активируйте виртуальное окружение:
```bash
source venv/bin/activate  # на macOS/Linux
# или
venv\Scripts\activate  # на Windows
```

5. Проверьте версию Python в окружении:
```bash
python --version
# Должно быть: Python 3.13.x
```

6. Обновите pip:
```bash
pip install --upgrade pip
```

7. Установите зависимости:
```bash
pip install gigachat pandas openpyxl python-docx
```

Если возникают проблемы с SSL сертификатами:
```bash
pip install --trusted-host pypi.org --trusted-host pypi.python.org --trusted-host files.pythonhosted.org gigachat pandas openpyxl python-docx
```

#### Вариант 2: Установка Python 3.13 через Homebrew (macOS)

Если Python 3.13 не установлен:

1. Установите Python 3.13 через Homebrew:
```bash
brew install python@3.13
```

2. Создайте виртуальное окружение:
```bash
cd ai_env
/opt/homebrew/bin/python3.13 -m venv venv
# или если Homebrew установлен в /usr/local:
# /usr/local/bin/python3.13 -m venv venv
```

3. Активируйте и установите зависимости (см. шаги 4-7 из Варианта 1).

#### Вариант 3: Использование pyenv (рекомендуется для управления версиями Python)

1. Установите pyenv (если ещё не установлен):
```bash
# macOS
brew install pyenv

# Linux
curl https://pyenv.run | bash
```

2. Установите Python 3.13 через pyenv:
```bash
pyenv install 3.13.7
```

3. Установите локальную версию для проекта:
```bash
cd ai_env
pyenv local 3.13.7
```

4. Создайте виртуальное окружение:
```bash
python -m venv venv
```

5. Активируйте и установите зависимости (см. шаги 4-7 из Варианта 1).

### Проверка установки

После установки проверьте, что всё работает:

```bash
source venv/bin/activate
python --version  # Должно быть Python 3.13.x
python -c "from gigachat import GigaChat; print('✓ GigaChat установлен успешно')"
```

Если видите сообщение "✓ GigaChat установлен успешно" — всё готово к работе!

## Получение ключа авторизации

1. Зайдите в личный кабинет Studio: https://developers.sber.ru/studio/
2. Откройте проект **GigaChat API**
3. Перейдите в раздел **"Настройки API"**
4. Нажмите **"Получить ключ"** и скопируйте Authorization Key

**Важно:** Ключ авторизации отображается только один раз. Сохраните его в безопасном месте.

## Использование

### Базовый запуск

```bash
python ai_analyzer.py ../report.xlsx --auth-key YOUR_AUTH_KEY
```

### Использование модели GigaChat-2-Pro (рекомендуется)

Для получения более качественного анализа используйте модель **GigaChat-2-Pro**:

```bash
python ai_analyzer.py ../report.xlsx --auth-key YOUR_AUTH_KEY --model GigaChat-2-Pro
```

### Дополнительные опции

**Только текстовый отчёт:**
```bash
python ai_analyzer.py ../report.xlsx --auth-key YOUR_AUTH_KEY --format txt
```

**Указать путь для сохранения отчёта:**
```bash
python ai_analyzer.py ../report.xlsx --auth-key YOUR_AUTH_KEY --output my_analysis
```

**Для юридических лиц (с проверкой SSL):**
```bash
python ai_analyzer.py ../report.xlsx --auth-key YOUR_AUTH_KEY --scope GIGACHAT_API_CORP --verify-ssl
```

## Доступные модели

### Модели второго поколения (рекомендуются)

- **GigaChat-2** (GigaChat 2 Lite) — быстрая и экономичная модель для базового анализа
- **GigaChat-2-Pro** — оптимальный баланс качества и стоимости, **рекомендуется для профессионального анализа**
- **GigaChat-2-Max** — максимальное качество для самых сложных задач, но выше стоимость токенов

### Модели первого поколения

Модели первого поколения (`GigaChat`, `GigaChat-Pro`, `GigaChat-Max`) автоматически перенаправляются на их аналоги второго поколения (`GigaChat-2`, `GigaChat-2-Pro`, `GigaChat-2-Max`) с сохранением стоимости.

## Выходные файлы

После выполнения анализа создаются следующие файлы:

- `ai_analysis_YYYYMMDD_HHMMSS.txt` — текстовый отчёт с полным анализом
- `ai_analysis_YYYYMMDD_HHMMSS.docx` — Word-документ с форматированным отчётом

### Структура отчёта

1. **Executive Summary** — краткая сводка для руководства:
   - Суть проблемы
   - Уровень критичности
   - Ключевые цифры и факты
   - Главные действия
   - Ожидаемые результаты

2. **Анализ угроз безопасности**:
   - Оценка уровня угрозы (низкий/средний/высокий/критический)
   - Идентификация типов атак
   - Конкретные IP или группы IP, требующие внимания
   - Признаки координированных атак или ботнетов
   - Оценка потенциального ущерба

3. **Влияние на бизнес**:
   - Влияние на пользовательский опыт и конверсию
   - Потери в SEO
   - Нагрузка на инфраструктуру и расходы
   - Риски для репутации бренда
   - Потенциальные финансовые потери

4. **Рекомендации**:
   - Немедленные действия (в течение часа)
   - Краткосрочные меры (1-7 дней)
   - Долгосрочная стратегия
   - Конкретные команды и правила (iptables/nginx/apache)

5. **Приложение: Статистика** — сводная информация из исходного отчёта

## Примеры использования

### Пример 1: Полный анализ с GigaChat-2-Pro

```bash
cd ai_env
source venv/bin/activate
python ai_analyzer.py ../www.trub-prom.com_report_20260112_145309.xlsx \
  --auth-key YOUR_AUTH_KEY \
  --model GigaChat-2-Pro
```

### Пример 2: Только текстовый отчёт

```bash
python ai_analyzer.py ../report.xlsx \
  --auth-key YOUR_AUTH_KEY \
  --model GigaChat-2-Pro \
  --format txt
```

### Пример 3: Сохранение с указанным именем

```bash
python ai_analyzer.py ../report.xlsx \
  --auth-key YOUR_AUTH_KEY \
  --model GigaChat-2-Pro \
  --output security_analysis_january
```

## Стоимость токенов

- Более продвинутые модели (Pro, Max) потребляют больше токенов и стоят дороже
- Количество использованных токенов отображается в консоли после каждого запроса
- Проверяйте баланс токенов в личном кабинете: https://developers.sber.ru/studio/

**Пример расхода токенов:**
- Анализ угроз: ~2000-3500 токенов
- Влияние на бизнес: ~900-1700 токенов
- Рекомендации: ~3000-3500 токенов
- Executive summary: ~3500-4500 токенов

**Итого:** ~10,000-13,000 токенов на полный анализ одного отчёта

## Устранение неполадок

### Ошибка: "библиотека gigachat не установлена"

```bash
pip install gigachat
```

### Ошибка: "Python 3.14 несовместим с Pydantic V1"

Используйте виртуальное окружение с Python 3.13, которое уже настроено в `ai_env/venv`.

### Ошибка: "401 Unauthorized" или "Ошибка авторизации"

- Проверьте правильность ключа авторизации
- Убедитесь, что ключ не истёк
- Получите новый ключ в личном кабинете

### Ошибка: "Недостаточно токенов"

- Пополните баланс токенов в личном кабинете
- Используйте более экономичную модель (GigaChat-2 вместо GigaChat-2-Pro)

### Ошибка: "Превышен лимит запросов"

- Подождите несколько минут и повторите попытку
- Скрипт автоматически повторяет запросы при временных ошибках

## Технические детали

### Архитектура

- **ReportAnalyzer** — загружает и парсит Excel-отчёты
- **GigaChatAnalyzer** — взаимодействует с GigaChat API для анализа
- **ReportGenerator** — генерирует текстовые и Word-отчёты

### Поддерживаемые форматы Excel

Скрипт ожидает Excel-файл со следующими листами:
- Сводка
- Подозрительные IP
- Статистика по странам
- Аномалии нагрузки
- IP из датацентров

### Обработка ошибок

- Автоматические повторные попытки при временных ошибках
- Детальные сообщения об ошибках для диагностики
- Graceful degradation при отсутствии опциональных зависимостей

## Лицензия и поддержка

Этот инструмент использует GigaChat API от Сбербанка. Для работы требуется:
- Регистрация в личном кабинете Studio
- Покупка токенов для использования API

Документация GigaChat API: https://developers.sber.ru/docs/ru/gigachat/
